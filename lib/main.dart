import 'bootstrap.dart';

// Main entry point of the application.
// It delegates startup configuration to bootstrap()
// to keep initialization logic separated and organized.
Future<void> main() async {
  await bootstrap();
}
